# Cloudflare Web Analytics beacon token.
# Get it from: Cloudflare Dashboard → Analytics → Web Analytics → Add site → copy token.
# Set this as a Pages environment variable (Settings → Environment variables) for production.
# For local dev, copy this file to .env.local and fill in the token.
VITE_CF_BEACON_TOKEN=
